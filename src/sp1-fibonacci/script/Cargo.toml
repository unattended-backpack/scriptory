[package]
version = "0.1.0"
name = "fibonacci-script"
edition = "2021"
default-run = "evm"

[[bin]]
name = "evm"
path = "src/bin/evm.rs"

[dependencies]
# Scriptory's evm.rs only generates and serializes proofs; verification
# happens server-side at Hierophant. We therefore omit sp1-sdk's
# `native-gnark` feature (and its CGO / gnark-go build dependency), since
# that feature only matters for the client.verify path.
# `network` became opt-in in sp1-sdk 6.x (default = []); the script drives
# proofs through NETWORK_RPC_URL -> NetworkProver, so it must be explicit.
# `reserved-capacity` (implies network) flips the 6.x NetworkProver default
# from Mainnet's auction flow to the Reserved/base flow Hierophant serves;
# without it, requests target auction RPCs Hierophant does not implement
# and auto-cancel.
sp1-sdk = { version = "=6.2.1", features = ["network", "reserved-capacity"] }
# The 6.x prove/setup surface is async.
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
clap = { version = "4.0", features = ["derive", "env"] }
# Used by create_proof_fixture in src/bin/evm.rs to dump a JSON fixture
# alongside each Plonk/Groth16 run. The fixture is for out-of-band
# Solidity verification; the output dir is gitignored so it never lands
# in a commit.
serde = { version = "1.0.200", default-features = false, features = ["derive"] }
serde_json = { version = "1.0", default-features = false, features = ["alloc"] }
hex = "0.4.3"
alloy-sol-types = { workspace = true }
fibonacci-lib = { path = "../lib" }

[build-dependencies]
sp1-build = "=6.2.1"
